import React from "react";
import './doctors_index.css';

const ProofOfConcept = () => {
  return (
    <div className="proof-of-concept">
      <div>The doctors and doctor locations, in conjunction with the map, are only a proof of concept as of now and currently display entities centered only around San Francisco, CA, regardless of where you (the user) are currently located.</div>
    </div>
  )
};

export default ProofOfConcept;